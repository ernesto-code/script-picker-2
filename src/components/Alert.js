import React from 'react';
import './alert.css';

function Alert(props) {
    return (
        <div className="alert">
            To make these scripts work, run them on the browser console. Please do not run Axe or any other automated tool after executing the scripts. 
        </div>
    );
}

export default Alert;